version: 2.1
agent:
  ssh:
    enabled: "true"
jobs:
  build:
    docker:
      - image: cimg/base:current
    resource_class: tabinol/local-kubernetes
    steps:
      - run: echo "Hi I'm on a Container Runner!"
workflows:
  build-workflow:
    jobs:
      - build
